# Web UI
streamlit>=1.36,<2.0

# ASR (Whisper на CTranslate2)
faster-whisper>=1.0.1,<2.0

# Работа со звуком (fallback для определения длительности и конверта)
pydub>=0.25.1,<1.0

# Базовые численные штуки
numpy>=1.26,<3.0
